services:
  - type: web
    name: fastapi-digitos
    env: python
    buildCommand: ""
    startCommand: bash -c "python -m uvicorn api.main:app --host 0.0.0.0 --port=$PORT"
    plan: free
    envVars:
      - key: PORT
        value: 10000
